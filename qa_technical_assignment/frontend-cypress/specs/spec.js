import specPage from "../page_objects/specPage";

describe('Test Set name', () => {
  before(() => {
  });

  beforeEach(() => {
  });

  it('Scenario 1 name', () => {
  });

  it('Scenario 2 name', () => {
  });

  after(() => {
  });

  afterEach(() => {
  });
});
